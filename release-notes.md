# 更新日志

## v1.6.0

### 新增功能

* 唤取分析新增「五星出货抽数走势」迷你趋势图：卡池卡与「全部卡池」汇总卡以面积折线呈现历次五星出货抽数与进行中的保底垫抽，悬停可查看各次出货抽数；对从未出货但已有垫抽的卡池也会绘制按垫抽定位的水平线，直观展示保底进度
* 设置新增「色温」切换（外观分区）：同一主题模式下支持 **暖白 / 冷白** 两套独立配色，浅 / 深各一套共 4 种组合；底色、卡片、输入框、窗口首帧底色统一联动，持久化到数据库，长时间观看可切到冷白缓解眼疲劳
* 设置新增「玻璃材质」开关：默认「关闭」（保持扁平实色），开启后并在已设置背景图时，标题栏 / 侧边栏 / 卡片 / 面板等对背景透出毛玻璃模糊效果
* 设置新增「背景亮度」滑杆（0 暗 → 100 亮，默认 50），并新增「隐藏图片 / 恢复默认」按钮，可随时调节背景图的明暗与显隐

### 体验优化

* 升级 Electron 至 v44，electron-builder 同步升级至 v26
* 「隐藏卡池」弹窗 UI 全面优化：改为两列紧凑网格，去除卡池条目的 hover 上浮 / 按压缩放动画，指示器换成 radio 风格圆点并做几何居中，字号与强调点按视觉平衡统一调整，整体更协调清爽
* 调整界面背景配色：浅色模式背景由 `#eef0f6` 调整为更明亮的 `#fafafa`，深色模式背景由 `#181a22` 调整为更深沉的 `#15161a`，整体观感更协调
* 重构唤取分析视图代码：将集中在 `gachaWuwa.js` 的渲染逻辑按职责拆分为独立视图模块（`bar` / `intuitive` / `table` / `detail` / `qizang` / `level` + `shared` 共享工具），降低单文件复杂度、便于维护

### 问题修复

* **修复内容区空白、按钮无数据的问题**：渲染进程以 `file://` 协议加载页面时，Chromium 同源策略禁止 `fetch` 访问本地视图 / 脚本文件，导致主内容区（唤取分析、卡池列表等）始终空白、侧边栏切换失效。改为注册 `local://` 自定义特权协议（启用 `supportFetchAPI`）将渲染资源以同源方式提供，并将页面与动态加载统一切换为 `local://` 绝对路径，彻底恢复内容正常渲染与数据加载
* 修复构建流水线 `prebuild` 在 `dist/` 不存在时误调用 `process.exit(0)` 提前终止、导致 `.build` 产物未生成、打包出的程序仍为旧逻辑的缺陷；改为仅在脚本直接运行时退出，被 `build-pipeline.js` 引入时正常返回

### 其他变更

* 新增运行期完整性自检模块（`src/core/security/selfcheck.js`）
* 建立构建加固流水线：入口改为 `.build/src/main.js`，构建前清理 → 混淆 renderer/core → 将 main/preload 编译为 V8 字节码 → 翻转 Electron Fuses + asarmor 加固；新增 `bytenode` / `javascript-obfuscator` / `@electron/fuses` / `asarmor` 依赖
* 数据库统一迁移至 `better-sqlite3`（12 → 13），移除 `sqlite3` / `ws` / `fs-extra` 等冗余依赖
* 引入代码规范与自动格式化：新增 ESLint（`lint` / `lint:fix`）与 Prettier（`format` / `format:check`）脚本
* 批量消除 ESLint 警告：给渲染进程跨文件共享函数/变量统一补 `/* exported */` 声明或行级豁免注释，局部 `prefer-const` 全部转 `const`，唯一一处真正死代码（`shared.js` 的 `isRolePool`）直接删除，`npm run lint` 从 30 warnings → 0 warnings 0 errors
* 清理 `gacha.js` / `gachaWuwa.js` 中无引用的旧渲染逻辑与未用变量，精简项目体积
* 清理本地 IDE 残留：删除被 `.gitignore` 忽略但在仓库内残留的 `.idea/` 私有配置目录与 5 个本地文件

## v1.5.0

### 新增功能

* 列表视图点击五星进度条即可展开查看本段抽数内的四星，四星按名称聚合、头像右上角显示数量角标（重复的四星一目了然）

### 体验优化

* 移除浅色 / 深色模式下的渐变光晕背景（极光背景层），界面更干净简洁
* 主题切换改为直接切换：去掉了球面扩散动画，切换更快、更稳定

### 问题修复

* 修复奇藏数据获取在特定情况下可能抛出的 `path` 未定义错误：补齐缺失的 `path` 模块引用，避免触发崩溃

### 其他变更

* 大幅清理未使用的代码与文件：移除未调用的函数、未使用的样式类、未引用的图表库（Chart.js）与空测试文件、空目录，精简项目体积、提升加载速度

## v1.4.0

### 新增功能

* 记住上次选中的账号：重启后自动恢复到上次退出的账号页面；删除账号后同步清除记忆，不再残留无效账号
* 条形视图五星新增「UP」标识，当期 UP 与歪一目了然

### 问题修复

* 修复列表视图下进度条长度失真的问题：进度条按实际比例分配宽度，抽数文字固定靠左、数字右对齐
* 修正限定唤取统计：统计覆盖全部限定卡池，未出 UP 不再显示占位文本；全部卡池角色按角色名合并显示个数

### 体验优化

* 优化账号识别与下拉框：奇藏同步回传游戏内 UID 供下拉框复用，避免偶发解析失败回退成通行证账号；下拉框与触发框等宽对齐、UID 完整显示，并新增「已解析 / 未解析」状态标签
* 优化等级养成表：差值对比的输入框移入卡片标题栏右侧，两种模式下卡片高度一致；输入框仅允许纯数字，清空按 0 计算；表格数字列等宽对齐
* 优化详情视图：上下页按钮改为纯图标样式、悬停变色；日期筛选按当天最高稀有度着色（五星金 / 四星紫 / 三星蓝）
* 同步结果浮窗改为自动隐藏（3.6 秒后自动消失），不再常驻遮挡界面
* 更新侧边栏「分析 / 奇藏 / 等级」图标：折线趋势图 / 五角星 / 阶梯上升折线

## v1.3.0

### 新增功能

* 唤取分析页新增「列表」视图：按卡池分组以横向进度条展示五星出货与当前已垫进度，信息密度更高，一眼看清各卡池垫抽情况
* 列表 / 卡片视图下，鼠标移到角色头像即可显示该角色唤取时间（提示框跟随鼠标，移出隐藏），无需进入详情页也能快速核对

### 体验优化

* 隐藏卡池弹窗重新设计：整体更紧凑（缩小弹窗宽度与卡片行高、收紧间距），勾选指示器同步缩小，视觉更协调

## v1.2.4

### 问题修复

* 设置页「鸣潮游戏目录」未手动设置时，自动显示程序探测到的游戏路径，并修复显示框显示的问题

### 其他变更

* 移除设置页「头像文件夹」入口（读取头像功能不受影响）

## v1.2.3

### 问题修复

* 修复「检查更新」误弹更新窗口、窗口标题版本显示为 `v?` 的问题：为更新事件增加版本比较守卫（仅当远端版本高于当前版本时才提示更新），并对异常空版本号用当前版本兜底，彻底消除误弹

## v1.2.2

### 问题修复

* 修复安装时偶发黑屏的问题：安装脚本原先在安装过程中强制结束 `explorer.exe`（资源管理器），会瞬间清空桌面与任务栏导致黑屏；改为只结束旧版本主进程（`feibijiubi.exe` / `FeibiJiubi.exe` / `菲比啾比.exe`），不再触碰资源管理器
* 修复「检查更新」偶发降级为「前往下载」的问题：更新请求补充 `User-Agent` 头以通过 GitHub 校验，且检查失败时自动重试一次；重试仍失败时保留自动更新能力并携带诊断信息（是否打包、app-update.yml 是否存在），不再直接降级为外链下载

### 其他变更

* 确保 `latest.yml` 与 `.blockmap` 随安装包一同发布，程序内自动更新（增量下载 + 静默安装）链路稳定可用

## v1.2.1

### 问题修复

* 修复「鸣潮内获取」在链接过期 / authkey 失效时仍遍历全部卡池才返回失败的问题，首次捕获致命鉴权错误立即中止并返回结果，响应更快
* 修复设置页「自定义配色」中「取消」「应用」按钮文字未居中的样式问题

### 其他变更

* 清理未使用的功能模块与依赖，精简项目体积，优化代码结构与可读性

## v1.2.0

### 新增功能

* 启动即自动列出库洛官方启动器登录过的全部账号（国服 + 国际服），无需手动操作
* 自动选中当前账号并实时同步奇藏 / 等级数据

### 问题修复

* 修复国际服账号在账号切换框中缺失的问题（不再按当前区服过滤）

## v1.1.0

### 体验优化

* 优化唤取分析页背景表现，移除易出异常的亮度调节，改为固定默认遮罩透明度，界面更稳定

### 其他变更

* 精简冗余样式与临时脚本，减小体积并提升加载速度

## v1.0.0

### 新增功能

* 首个正式版发布：菲比啾比 · 鸣潮唤取（抽卡）分析工具
* 唤取记录本地读取与多维度统计分析：星级分布、出货概率、垫抽 / 歪率、卡池详情与差值对比
* 奇藏 / 等级数据同步
* 极光玻璃 UI，支持深色 / 浅色主题与自定义主色
* 自动检查更新（GitHub Releases）
