# 更新日志

## v1.4.0
### 新增功能

* 记住上次选中的账号：重启后自动恢复到上次退出的账号页面；删除账号后同步清除记忆，不再残留无效账号
* 条形视图五星新增「UP」标识，当期 UP 与歪一目了然

### 问题修复

* 修复列表视图下进度条长度失真的问题：1 抽与 5 抽不再显示为等长，进度条按实际比例分配宽度，抽数文字固定靠左、数字右对齐
* 修正限定唤取统计：统计覆盖全部限定卡池，未出 UP 不再显示占位文本；全部卡池角色按角色名合并显示个数

### 体验优化

* 优化账号识别与下拉框：奇藏同步回传游戏内 UID 供下拉框复用，避免偶发解析失败回退成通行证账号；下拉框与触发框等宽对齐、UID 完整显示，并新增「已解析 / 未解析」状态标签
* 优化等级养成表：差值对比的输入框移入卡片标题栏右侧，两种模式下卡片高度一致；输入框仅允许纯数字，清空按 0 计算；表格数字列等宽对齐
* 优化详情视图：上下页按钮改为纯图标样式、悬停变色；日期筛选按当天最高稀有度着色（五星金 / 四星紫 / 三星蓝）
* 同步结果浮窗改为自动隐藏（3.6 秒后自动消失），不再常驻遮挡界面
* 更新侧边栏「分析 / 奇藏 / 等级」图标：折线趋势图 / 五角星 / 阶梯上升折线

## v1.3.0
### 新增功能

* 唤取分析页新增「列表」视图：按卡池分组以横向进度条展示五星出货与当前已垫进度，信息密度更高，一眼看清各卡池垫抽情况
* 列表 / 卡片视图下，鼠标移到角色头像即可显示该角色唤取时间（提示框跟随鼠标，移出隐藏），无需进入详情页也能快速核对

### 体验优化

* 隐藏卡池弹窗重新设计：整体更紧凑（缩小弹窗宽度与卡片行高、收紧间距），勾选指示器同步缩小，视觉更协调

## v1.2.4
### 问题修复

* 设置页「鸣潮游戏目录」未手动设置时，自动显示程序探测到的游戏路径，并修复显示框显示的问题

### 其他变更

* 移除设置页「头像文件夹」入口（读取头像功能不受影响）

## v1.2.3
### 问题修复

* 修复「检查更新」误弹更新窗口、窗口标题版本显示为 `v?` 的问题：为更新事件增加版本比较守卫（仅当远端版本高于当前版本时才提示更新），并对异常空版本号用当前版本兜底，彻底消除误弹

## v1.2.2
### 问题修复

* 修复安装时偶发黑屏的问题：安装脚本原先在安装过程中强制结束 `explorer.exe`（资源管理器），会瞬间清空桌面与任务栏导致黑屏；改为只结束旧版本主进程（`feibijiubi.exe` / `FeibiJiubi.exe` / `菲比啾比.exe`），不再触碰资源管理器
* 修复「检查更新」偶发降级为「前往下载」的问题：更新请求补充 `User-Agent` 头以通过 GitHub 校验，且检查失败时自动重试一次；重试仍失败时保留自动更新能力并携带诊断信息（是否打包、app-update.yml 是否存在），不再直接降级为外链下载

### 其他变更

* 确保 `latest.yml` 与 `.blockmap` 随安装包一同发布，程序内自动更新（增量下载 + 静默安装）链路稳定可用

## v1.2.1
### 问题修复

* 修复「鸣潮内获取」在链接过期 / authkey 失效时仍遍历全部卡池才返回失败的问题，首次捕获致命鉴权错误立即中止并返回结果，响应更快
* 修复设置页「自定义配色」中「取消」「应用」按钮文字未居中的样式问题

### 其他变更

* 清理未使用的功能模块与依赖，精简项目体积，优化代码结构与可读性

## v1.2.0
### 新增功能

* 启动即自动列出库洛官方启动器登录过的全部账号（国服 + 国际服），无需手动操作
* 自动选中当前账号并实时同步奇藏 / 等级数据

### 问题修复

* 修复国际服账号在账号切换框中缺失的问题（不再按当前区服过滤）

## v1.1.0
### 体验优化

* 优化唤取分析页背景表现，移除易出异常的亮度调节，改为固定默认遮罩透明度，界面更稳定

### 其他变更

* 精简冗余样式与临时脚本，减小体积并提升加载速度

## v1.0.0
### 新增功能

* 首个正式版发布：菲比啾比 · 鸣潮唤取（抽卡）分析工具
* 唤取记录本地读取与多维度统计分析：星级分布、出货概率、垫抽 / 歪率、卡池详情与差值对比
* 奇藏 / 等级数据同步
* 极光玻璃 UI，支持深色 / 浅色主题与自定义主色
* 自动检查更新（GitHub Releases）
